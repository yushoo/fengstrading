# Google Sheets Integration
# Create a Google Sheet with tabs: Shows, Arrivals, Updates
# Publish each tab via File > Share > Publish to web (CSV format)
# Then paste your sheet ID (from the URL) below:
VITE_GOOGLE_SHEET_ID=your_sheet_id_here
